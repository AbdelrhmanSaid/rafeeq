// Settings tab ids, in display order. Kept in a leaf module so the router can
// validate /settings/:tab without pulling the settings views into its bundle.
export const SETTINGS_TAB_IDS = ['appearance', 'prayer', 'quran', 'azkar', 'app', 'downloads']
